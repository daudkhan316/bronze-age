{
  "name": "bronze-age",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Bronze Age — a browser RTS inspired by Age of Empires II. Phase 0: skeleton.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
